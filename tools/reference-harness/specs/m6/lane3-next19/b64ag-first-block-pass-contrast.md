# Lane 3 Next19 b64ag First-Block Pass Contrast

Status: Tier C diagnostic. This step does not flip M6, does not promote
`linear_propagator`, does not relax comparator tolerance, does not inject
retained final solution samples, and does not set
`full_eta_zero_contour_applied=true`.

## Step Chosen

Option (c): compare the two direct b64ag first-block rows that still fail the
50-digit comparator against the four nearby prefix/selected rows that pass in
the current 39-coefficient packet.

This lane adds a repeatable fail-closed classifier:

```text
tools/reference-harness/scripts/diagnose_b64ag_first_block_gap.py
```

The script consumes the 50-digit comparator JSON and emits the row
classification used here. It has a CTest self-check so future edits keep the
eight direct-row failures and the four passing contrast rows separated.

## Fresh Diagnostic

Input comparator:

```text
/tmp/autoibp_orch/exec/lane1_iter20_b64ag/full-stripped-compare50.json
```

Lane artifact:

```text
/tmp/autoibp_orch/exec/iter_lane3_next19/b64ag-first-block-gap-diagnostic.json
```

Summary:

```text
STEP_CHOSEN=c
PASSED_50=false
PASSED_COUNT_50=31/39
MIN_DIGIT_AGREEMENT_AFTER=37
DIRECT_FIRST_BLOCK_FAILURES=8
CONTRAST_FAILURES=0
M6_FLIPPED=false
```

## Row Contrast

| Target | Classifier path | 50-digit result | Minimum component digits | Failed orders |
| --- | --- | ---: | ---: | --- |
| `gauge[1,1,1,-1,1,0,0,0,0]` | live first-block fit | 0/4 | 37 | eps^-1, eps^0, eps^1, eps^2 |
| `gauge[1,1,1,0,1,0,0,0,0]` | live first-block fit | 0/4 | 37 | eps^-1, eps^0, eps^1, eps^2 |
| `gauge[0,1,1,1,1,-1,0,0,0]` | zero prefix row | 3/3 | 999 | none |
| `gauge[0,1,1,1,1,0,0,0,0]` | zero prefix row | 3/3 | 999 | none |
| `gauge[1,1,1,1,1,-1,0,0,0]` | reviewed downstream target table | 5/5 | 999 | none |
| `gauge[1,1,1,1,1,0,0,0,0]` | reviewed downstream target table | 5/5 | 999 | none |

The two failing rows are the only contrasted rows still generated by the live
first-block endpoint transport plus first-block Frobenius branch, raw retained
target reduction, and post-endpoint Laurent fit. They do not use the
`NormalizeMat` target switch.

The four passing contrast rows differ in kind:

- The two `gauge[0,1,1,1,1,...]` rows are zero/implicit-zero prefix rows in this
  packet, so they do not exercise a high-precision live first-block fit.
- The two downstream selected rows are emitted through reviewed downstream
  target Laurent row publication, so this artifact compares the reviewed table
  text against the retained AMFlow row text and records sentinel `999` digit
  agreement.

Therefore these four passing rows do not contradict the remaining gap. They
bypass the exact path that still limits the two direct first-block rows.

## Conclusion

The current 8-coefficient gap is localized to direct first-block rows whose
published C++ coefficients come from the live endpoint/Frobenius transport and
post-endpoint epsilon fit, then compare against the retained AMFlow golden text.
The contrasted passers either compare as zero or use reviewed table publication,
so they do not provide evidence that the direct first-block live-fit path has a
50-digit AMFlow reference. A valid 50-digit closure still needs a recaptured or
otherwise independently reviewed high-precision AMFlow reference for the two
direct rows, not rounding or truncation of the C++ output.

## Four-Role Review

- Implementer: APPROVE Tier C. The diagnostic is read-only against comparator
  evidence and keeps the two direct live-fit rows separate from zero/table
  passers.
- Test: APPROVE Tier C. The new self-check is wired into CTest and the fresh
  diagnostic reports `31/39` at 50 digits with eight direct-row failures.
- Physics/source: APPROVE Tier C. The row classes match the runtime path: direct
  first-block fit versus zero prefix rows and reviewed downstream publication.
- Anti-fake: APPROVE Tier C. No comparator tolerance, golden, runtime
  coefficient, qualifier, or M6 metadata was changed.

## Honest Status

`PASSED_50=false`, `MIN_DIGIT_AGREEMENT_AFTER=37`, and `M6_FLIPPED=false`.
